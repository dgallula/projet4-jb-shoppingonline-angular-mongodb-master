import { ExpirationDateDirective } from './expiration-date.directive';

describe('ExpirationDateDirective', () => {
  it('should create an instance', () => {
    const directive = new ExpirationDateDirective();
    expect(directive).toBeTruthy();
  });
});
